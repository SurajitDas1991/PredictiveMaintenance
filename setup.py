from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.0.1',
    description='The dataset consists of 10 000 data points stored as rows with 6 features in columns. The aim is to find if an item can fail or not ',
    author='Surajit Das',
    license='MIT',
)
